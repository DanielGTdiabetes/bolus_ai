# ML Training Output

This directory stores offline residual training artifacts generated by
`backend/scripts/train_residual_forecast.py`.

Each run creates a versioned subdirectory with models, metrics, and reports.
