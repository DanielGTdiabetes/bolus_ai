[pytest]
asyncio_mode = auto
addopts = -q --ignore-glob=test_*.txt
testpaths =
    tests
    ../tests
norecursedirs =
    .git
    __pycache__
    .venv
    node_modules
    var
    .runtime
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
