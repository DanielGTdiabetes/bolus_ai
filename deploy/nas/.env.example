# ==========================================
# ARCHIVO .ENV DE EJEMPLO PARA NAS (deploy/nas)
# Renombra esto a .env o copia el contenido a Portainer
# ==========================================

# --- CONFIGURACIÓN DE BASE DE DATOS LOCAL (NAS) ---
POSTGRES_USER=admin
POSTGRES_PASSWORD=tu_password_segura
POSTGRES_DB=bolus_ai

# URL interna para la App (debe coincidir con las credenciales de arriba)
# Formato: postgresql://USER:PASSWORD@db:5432/DB_NAME
# Nota: '@db' es el nombre del servicio interno en Docker, no lo cambies.
DATABASE_URL=postgresql://admin:tu_password_segura@db:5432/bolus_ai

# --- DIRECTORIOS Y RUTAS (PERSISTENCIA EN NAS) ---
# Ajusta estas rutas a las carpetas reales de tu NAS donde quieras guardar los datos
DB_DATA_PATH=/volume1/docker/bolus_ai/db_data
APP_DATA_PATH=/volume1/docker/bolus_ai/app_data

# Puertos expuestos en el NAS
APP_PORT=8000
DB_PORT=5433

# --- VARIABLES DE LA APLICACIÓN (Copiadas de Render/Producción) ---
DATA_DIR=/app/backend/data
# IP o Dominio con el que accedes al NAS desde fuera
RENDER_EXTERNAL_URL=http://192.168.1.XXX:8000

# Claves Secretas (Genera cadenas aleatorias largas para producción)
APP_SECRET_KEY=cambia_esto_por_una_cadena_larga_aleatoria
JWT_SECRET=cambia_esto_por_otra_cadena_larga_aleatoria
ALLOWED_TELEGRAM_USER_ID=123456789

# --- NIGHTSCOUT (OBLIGATORIO PARA GLUCOSA) ---
# Faltaban en tu lista original, pero son necesarias para ver la glucosa.
NIGHTSCOUT_URL=https://tu-sitio-nightscout.herokuapp.com
NIGHTSCOUT_API_SECRET=tu_api_secret_de_nightscout

# --- TELEGRAM BOT ---
ENABLE_TELEGRAM_BOT=true
TELEGRAM_BOT_TOKEN=tu_bot_token_aqui
TELEGRAM_DEFAULT_CHAT_ID=123456789
TELEGRAM_WEBHOOK_SECRET=clave_secreta_webhook

# --- INTEGRACIONES IA (Google Gemini) ---
VISION_PROVIDER=gemini
GEMINI_MODEL=gemini-1.5-flash
GEMINI_MODEL_PRO=gemini-1.5-pro
GOOGLE_API_KEY=tu_api_key_de_google

# --- TECLA DE INGESTIÓN (Integraciones) ---
NUTRITION_INGEST_KEY=clave_para_atajos_ios

# --- SINCRONIZACIÓN Y BACKUP (OPCIONAL) ---
# URL de tu base de datos antigua en la nube (Neon) para backups de failover
CLOUD_DATABASE_URL=postgresql://user:pass@ep-xyz.neon.tech/neondb
# Activar sincronización diaria (0 = No, 1 = Si)
SYNC_ENABLED=0
SYNC_INTERVAL_SECONDS=86400
